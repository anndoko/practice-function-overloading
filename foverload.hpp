#ifndef foverload_hpp
#define foverload_hpp

#include <stdio.h>

int max(int, int);
int max(int, int, int);
int max(int*, int);
int max(float, int);
int max(int,float);


#endif /* foverload_hpp */
